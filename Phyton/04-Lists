


#Lists

primes=[2, 3, 5, 7]
print(primes)
print(type(primes))


planets = ['Mercury', 'Venus', 'Earth', 'Mars', 'Jupiter', 'Saturn', 'Uranus', 'Neptune']
print(planets)
print(type(planets))


#We can make a list of lists:

hands = [
    ['J', 'Q', 'K'],
    ['2', '2', '2'],
    ['6', 'A', 'K'], # (Comma after the last element is optional)
]
print(hands)
print(type(hands))

hands = [['J', 'Q', 'K'], ['2', '2', '2'], ['6', 'A', 'K']]


#A list can contain a mix of different types of variables:

my_favourite_things = [32, 'raindrops on roses', help]
print(my_favourite_things)


#Indexing

print(planets[0])
print(planets[1])
print(planets[-1])
print(planets[-2])


#Slicing

print(planets[0:3])  #first three planets
print(planets[:3])   

print(planets[3:])

print(planets[1:-1])  # All the planets except the first and last

print(planets[-3:]) # The last 3 planets


#Changing lists

planets[3] = 'Malacandra'
print(planets)

planets[:3] = ['Mur', 'Vee', 'Ur']
print(planets)
planets[:4] = ['Mercury', 'Venus', 'Earth', 'Mars',]
print(planets)


#List functions

#len():
print(len(planets))   # How many planets are there?

#sorted():
print(sorted(planets))   # The planets sorted in alphabetical order

#sum():
print(sum(primes))

#min()/ max():
print(min(primes))
print(max(primes))



#List methods

print(planets.append('Pluto'))

#list.pop(): list.pop removes and returns the last element of a list:
print(planets.pop())
print(planets)

# Bir deste kartımız olsun
kartlar = ["As", "Papaz", "Kız", "Vale"]

# En sondaki "Vale" kartını çekiyoruz (pop yapıyoruz)
cekilen_kart = kartlar.pop()

print("Çekilen Kart:", cekilen_kart)  # Çıktı: Vale
print("Kalan Kartlar:", kartlar)     # Çıktı: ['As', 'Papaz', 'Kız']


#Searching lists: list.index()

print(planets.index('Earth'))

# Is Earth a planet?
print("Earth" in planets)
# Is Calbefraques a planet?
print("Calbefraques" in planets)


#Tuples: Yeni bir eleman eklenemez, çıkarılamaz, değiştirilemez. () ile oluşturulur.
t = (1, 2, 3)

#as_integer_ratio():
sayi = 0.25
kesir_hali = sayi.as_integer_ratio()

print(kesir_hali)       # Çıktı: (1, 4)  --> İşte bu bir Tuple!
print(type(kesir_hali)) # Çıktı: <class 'tuple'>

x = 0.125
print(x.as_integer_ratio())

numerator, denominator = x.as_integer_ratio()
print(numerator / denominator)


a = 1
b = 0
a, b = b, a
print(a, b)



#Question_1: Return the second element of the given list. If the list has no second element, return None.

L=[1,5,6,8,3]

def select_second(L):
    if len(L)>=2:
        return L[1]
    else:
        return None

print(select_second(L))



#Question_2: Given a list of teams, where each team is a list of names, return the 2nd player (captain) from the last listed team

L = [['Paul', 'John', 'Ringo', 'George']]
def losing_team_captain(teams):
    return teams[-1][1]

print(losing_team_captain(L))



#Question_3: It warps the last place racer into first place and the first place racer into last place.
racers =["Mario", "Bowser", "Luigi"]

def purple_shell(racers):
    racers[0], racers[-1] = racers[-1], racers[0]
    return 

purple_shell(racers)
print(racers)



#Question_4: The lengths of the following lists.

a = [1, 2, 3]
b = [1, [2, 3]]
c = []
d = [1, 2, 3][1:]

print(len(a))
print(len(b))
print(len(c))
print(len(d))



#Question_5: A guest is considered 'fashionably late' if they arrived after at least half of the party's guests.
#The function below which takes a list of party attendees as well as a person, and tells us whether that person is fashionably late.

arrivals = ['Adela', 'Fleda', 'Owen', 'May', 'Mona', 'Gilbert', 'Ford']

def fashionably_late(arrivals,name):
    order = arrivals.index(name)
    return order >= len(arrivals) / 2 and order != len(arrivals)-1

print(fashionably_late(arrivals,'Mona'))

